# Cheque Harmony

A modern cheque management application built with React, Vite, and Google Gemini AI.

## 🚀 How to Deploy (Bangla Guide)

অ্যাপটি লাইভ (Live) করার জন্য নিচের ধাপগুলো অনুসরণ করুন:

### ধাপ ১: সেটআপ
আপনার কম্পিউটারে Node.js ইন্সটল থাকতে হবে। এরপর প্রজেক্ট ফোল্ডারে টার্মিনাল ওপেন করে লিখুন:
```bash
npm install
```

### ধাপ ২: বিল্ড তৈরি করা
অ্যাপটি প্রোডাকশনের জন্য রেডি করতে লিখুন:
```bash
npm run build
```
এটি সফল হলে আপনার ফোল্ডারে `dist` নামে একটি নতুন ফোল্ডার তৈরি হবে।

### ধাপ ৩: পাবলিশ করা (Netlify)
১. [Netlify Drop](https://app.netlify.com/drop) লিঙ্কে যান।
২. আপনার `dist` ফোল্ডারটি টেনে ব্রাউজারে ছেড়ে দিন।
৩. কয়েক সেকেন্ডেই আপনার অ্যাপ লাইভ হয়ে যাবে!

### ধাপ ৪: কনফিগারেশন (Google Sheet & API)
লাইভ সাইটে যাওয়ার পর:
- **Settings** পেজে যান।
- **Connection** বক্সে আপনার Google Apps Script URL টি দিন।
- OCR ফিচারের জন্য Netlify-র সেটিংসে `API_KEY` নামে এনভায়রনমেন্ট ভেরিয়েবল সেট করুন।

---

## 🛠 Local Development

1. Install dependencies:
   ```bash
   npm install
   ```

2. Create a `.env` file in the root directory and add your Gemini API Key:
   ```
   API_KEY=your_api_key_here
   ```

3. Start the development server:
   ```bash
   npm run dev
   ```
